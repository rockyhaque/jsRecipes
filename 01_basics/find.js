// find selects elements based on a condition and returns first elemenets that fulfilled the condition.

const players = [23, 25, 21, 54, 45];

const selected = players.find(p => p > 20);
console.log(selected);