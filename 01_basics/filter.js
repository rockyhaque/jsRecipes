// filter selects elements based on a condition and returns an array with the elemenets that fulfilled the condition.

const players = [23, 25, 21, 54, 45];

const selected = players.filter(p => p > 300);
console.log(selected);